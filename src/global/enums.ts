export enum OptionSelectStyle {
  INDIGO = "INDIGO",
  CYAN = "CYAN",
  GRAY = "GRAY"
}

export enum UuidOptions {
  SINGLE = "Single Uuid",
  MULTIPLE = "Multiple Uuids",
}

export enum UuidCopyOptions {
  REGULAR = "Basic Format",
  SQL = "SQL Format",
}
